/**
 * Константы приложения (не включая actionTypes)
 * @module constants
 * @flow
 */

// урл для мока сервера сообщений
export const WS_SERVER_URL: string = 'ws://superchat.com';

// высота отдельного пользователя в списке
export const USER_HEIGHT: number = 50;
